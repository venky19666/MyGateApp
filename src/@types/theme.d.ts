export { }

declare global {
    type ExtendedTheme = {
        primary: string,
        secondary: string,
        background: string,
        color: string,
        error: string,
        warning: string,
        success: string
    }
}